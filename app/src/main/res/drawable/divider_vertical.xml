<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Divider height -->
    <size
        android:height="1dp"
        android:width="1dp" />
    <!-- Inset from the left & right edges -->
    <solid android:color="?attr/colorOutline" />
    <padding
        android:left="16dp"
        android:right="16dp" />
</shape>
